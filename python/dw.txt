

vscode 에서는 실행을 할떄  폴더 선택 후 해당(선택한) 맨 위 디렉토리 에서 작업 및 실행을 진행해야 경로 문제가 생기지 않음






	pack()  은 실제로 메모리엔 어떻게 저장될까?



파이썬의 struct 는 c와는 좀다륽?

open( 'wb' ) 
           ㄴ 2진 파일 형태로 쓸수 있음 , 읽기는 rb!?


	pack('H',  )
        	        ㄴ H Salt 

 글자가  float()라서  예외처리를 한다음 그 안에 끝내버리지 않고 추가 작업을 해줄 수 있음
		.write()
[2, 0.8, 'a b'] 
	 실수든 정수든 문자열이든 모든 타입을 문자열 ' ' 로 바꿔서 리스트에 넣을 숟 있고 
['ab', 0.89]
 	 두가지 형태만 고려해ㅓ
 ㄴ             실수가 아니니    ['H' , 2  , 'a' ,  'b'] 
				  ㄴ write() 로 a와  b를 출력해냄 
			         ㄴ 바이트 크기

			    ㄴ 문자 표시


클래스를 만든 다음 함수를 만들어서 
클래스의 요소로 해당되는 인스턴스를 한줄한줄을 다 넣어서 만들어서
	그걸 이용해서 출력을 해낼수 있음

		self , 



파라미터를 넘길때  level=0.7 로 넘겨서 유지보수를 편하게!??



   join().map(   ) 



[ [         ]  
  [        ] 
write( b'L' +  len(line).to_bytes(4,  'big
			          ㄴ 뒤쪽이 큰 메모리? - 빅 인디안!? // 반대는 리틀 인디언 'little
				 	 예  00, 00, 00, 23
			ㄴ 바이트 크기로 바꿈 ┘
				0x23
			 ㄴ 4개를 
	ㄴ L로 이름을 정해주기 위해  b'L'  	


	isinstance(item, str) 로 item  의 형태를 익어서 

	str(item0.encode(utf 8)

	decode()
       form_bytes()






인코딩을 유니코드로....
	 ㄴ 읽는쪽 쓰는쪽 양쪽에서 규약한것 이라 제각기 다른 방법으로 표현 가능.

 dump()?!



바이너리 코드는 메모리를 작게 만들고 간단한 숫자로 만들어 처리가 빠르지만 그대로 사람이 읽을수는 없음






				






